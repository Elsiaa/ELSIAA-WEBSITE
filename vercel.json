{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "tanstack-start",
  "installCommand": "bun install --cwd app",
  "buildCommand": "bun run --cwd app build && rm -rf .vercel/output && mkdir -p .vercel && cp -R app/.vercel/output .vercel/output",
  "bunVersion": "1.x"
}
